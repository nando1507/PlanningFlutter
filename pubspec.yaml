name: planningpoker
description: Projeto para fazermos Plannings

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  google_fonts: ^2.0.0
  flutter_svg: ^0.22.0
  fl_chart: ^0.35.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  

flutter:
  uses-material-design: true

assets:
  - assets/images/
  